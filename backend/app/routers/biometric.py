"""
Router: Biometric Uploads
Endpoints: POST /api/uploads/biometric, GET /api/uploads, GET /api/uploads/{id}
Phase 3: Full implementation pending.
"""
from fastapi import APIRouter

router = APIRouter(
    prefix="/api/uploads",
    tags=["biometric"],
)

# Endpoints will be implemented in Phase 3
